import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lgu.tenantdashboard',
  appName: 'Tenant Dashboard',
  webDir: 'out'
};

export default config;
